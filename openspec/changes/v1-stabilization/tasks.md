# Tasks: v1-stabilization — Phase 0 audit + Phase 1 CI/build integrity

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | ~350–450 authored + ~610 pure deletion (04a) |
| 400-line budget risk | High |
| Chained PRs recommended | Yes |
| Suggested split | Tracker (draft) → 01 → 02 → 03 → 04a → 04b → 05 |
| Delivery strategy | force-chained |
| Chain strategy | feature-branch-chain |

Decision needed before apply: No
Chained PRs recommended: Yes
Chain strategy: feature-branch-chain
400-line budget risk: High

Topology (`main` e6f6dd6 is read-only; only the tracker merges to main):

```
main e6f6dd6 -> feat/v1-stabilization (draft tracker)
  -> 01-audit-contract -> 02-ci-contract -> 03-build-release
  -> 04a-receiver-dedup -> 04b-receiver-runner -> 05-metadata
```

Worktrees: `/home/cristian/.cache/michi-v1s/wt-tracker`, `wt-01`..`wt-05`, shared `CARGO_TARGET_DIR=/home/cristian/.cache/michi-v1s/target`. Child PR base = immediate parent branch; if a child diff shows previous slices, retarget/rebase until clean. `.github/workflows/ci.yml` is shared by 02 (add job) and 03 (platforms) — chain order prevents conflict; never reorder. Guard on every unit: no unrelated formatting/refactor; diff limited to listed files.

### Suggested Work Units

| Unit | Goal | Likely PR (base) | Focused test command | Runtime harness | Rollback boundary |
|------|------|-------------------|----------------------|-----------------|-------------------|
| 0 | Tracker + planning artifacts | tracker draft (main) | `git worktree list`; `git -C wt-tracker status --porcelain` empty | N/A — commands/checks only, no runtime | `git worktree remove`; nothing pushed |
| 01 | Audit + Player contract repair | PR 01 (tracker) | `python3 -m py_compile tests/e2e/test_player_micro_contract_compatibility.py`; `cargo test --workspace` | boot binary on scratch port + throwaway admin; contract run; log captured | revert test/doc; delete audit doc |
| 02 | CI contract gate | PR 02 (01) | `bash scripts/ci_contract_gate.sh` exit 0; failure paths non-zero | gate boots real server on isolated path/port, health wait, contract, trap cleanup | remove job + script |
| 03 | Docker/toolchain/ARM64 | PR 03 (02) | `cargo test --workspace`; `docker build` + smoke | local Docker boot (0.2.0, 37 migrations, healthy, WAL, cleanup); buildx arm64 emulated boot | revert Dockerfile/ci.yml; delete rust-toolchain.toml |
| 04a | Dedup (pure deletion) | PR 04a (03) | `cargo test --workspace` → 217/0/14 unchanged | N/A — files never compiled; deletion-only | restore deleted files |
| 04b | Receiver runner repair | PR 04b (04a) | `bash scripts/test_receiver_e2e.sh` sims-up green / sims-down non-zero | run sims via `scripts/run_receiver_sim_*.sh` with env paths, then runner | revert scripts/docs |
| 05 | Metadata truthfulness | PR 05 (04b) | `grep -n '^version:' casaos/data.yml` = 0.2.0; `grep -c 'fn migration_0' crates/michi-db/src/lib.rs` = 37; CHANGELOG 37 | N/A — two metadata lines, no runtime | revert two files |

## Work Unit 0 — Tracker / planning (PR: tracker draft, base: main e6f6dd6)

- [x] **0.1** Create detached tracker worktree from exact HEAD — Deps: none · Files: `/home/cristian/.cache/michi-v1s/wt-tracker` · Evidence: `git worktree add -b feat/v1-stabilization /home/cristian/.cache/michi-v1s/wt-tracker e6f6dd6f1b043ba9483614027bee2284586cf4ef`; `git -C wt-tracker rev-parse HEAD` = e6f6dd6…; `git -C wt-tracker status --porcelain` empty; main tree untouched · Rollback: `git worktree remove wt-tracker` · ~0 authored lines · Commit: none yet (no branch/commit/PR in this phase) · Base: main (read-only)
- [x] **0.2** Bring ONLY planning paths into tracker — Deps: 0.1 · Files: copy `openspec/config.yaml` + `openspec/changes/v1-stabilization/**` into wt-tracker (never `git add openspec/` or `-A`; no WebUI/static/tests/api.rs files) · Evidence: `git -C wt-tracker add openspec/config.yaml openspec/changes/v1-stabilization`; `git diff --cached --name-only` lists exactly those paths; `git status --porcelain` clean after `git commit -m "docs(openspec): track v1-stabilization planning artifacts"` · Rollback: `git reset --hard` on tracker before push; nothing pushed · ~0 authored (planning artifacts) · Commit: `docs(openspec): track v1-stabilization planning artifacts` · Base: feat/v1-stabilization

## Work Unit 1 — Slice 01: Audit + authenticated Player contract (PR 01, base: tracker)

- [x] **1.1** Author `docs/V1_STABILIZATION_AUDIT.md` — Deps: 0.2 · Files: `docs/V1_STABILIZATION_AUDIT.md` (new, ~200 lines) · Evidence: exact HEAD `e6f6dd6f1b…`; four green gates with exit 0 (fmt/check/test/clippy); 217 pass/0 fail/14 ignored; Docker smoke 200 + 37 migrations; P0=0 per user severity model; complete 14-test `#[ignore]` inventory with behavior per test (`crates/michi-receivers/tests/receiver_simulator_integration.rs`), explicitly unverified-not-passing; P1.1–P1.4 inventory with file:line; working-tree WebUI labeled uncommitted/contaminated · Rollback: delete file · ~200 authored · Commit: `docs(audit): add evidence-backed v1-stabilization baseline audit` · Base: 01-audit-contract
- [x] **1.2** Fix Python parse fault without global BASE_URL mutation — Deps: 1.1 · Files: `tests/e2e/test_player_micro_contract_compatibility.py` (remove `global BASE_URL` at :71; thread base_url explicitly — e.g. `main(base_url, token)` + `test()`/harness param, per design D4) · Evidence: `python3 -m py_compile tests/e2e/test_player_micro_contract_compatibility.py` exit 0; `grep -n "global BASE_URL"` empty · Rollback: revert file · ~6 authored · Commit: `fix(contract): repair player-micro contract test syntax and auth harness` · Base: 01-audit-contract
- [x] **1.3** Swap phantom field assert (Option A) — Deps: 1.2 · Files: `tests/e2e/test_player_micro_contract_compatibility.py:84` — replace `assert info.get("michi_link_version") == "1.0.0-alpha"` with `assert info.get("api_version") == "v1"` · Evidence: test asserts api_version; NO server change — `git diff -- crates/michi-api crates/michi-link` empty; `V1ServerInfo` untouched · Rollback: revert line · ~2 authored · Commit: `fix(contract): assert api_version instead of phantom michi_link_version` · Base: 01-audit-contract
- [x] **1.4** Implement ordered admin auth flow + loud failures — Deps: 1.3 · Files: `tests/e2e/test_player_micro_contract_compatibility.py` — sequence: (1) GET public `/api/v1/server/info` no auth → 200 + api_version/service/auth/features asserts; (2) POST `/api/auth/login` with throwaway admin from `MICHI_AUTH_USERNAME`/`MICHI_AUTH_PASSWORD`; (3) extract and require token; (4) `Authorization: Bearer <token>` on protected requests: import preflight new+legacy fixtures, queue transfer empty-body 400, diagnostics `player_compatibility`, playback state shape · Evidence: missing creds / login failure / absent-invalid token / any protected 401 → FAIL recorded and exit non-zero, NEVER SKIP; happy path exit 0 with FAIL=0 · Rollback: revert file · ~30 authored · Commit: `fix(contract): authenticate admin and bear-ify protected contract checks` · Base: 01-audit-contract
- [x] **1.5** Correct stale docs — Deps: 1.3 · Files: `docs/MICHI_LINK_MICRO_E2E.md:10` — `michi_link_version: "1.0.0-alpha"` → `api_version: "v1"` · Evidence: `grep -n api_version docs/MICHI_LINK_MICRO_E2E.md` line 10; no other doc lines changed · Rollback: revert line · ~1 authored · Commit: `docs(contract): correct version authority to api_version in E2E doc` · Base: 01-audit-contract
- [x] **1.6** Local real-server harness proof — Deps: 1.4 · Files: none (evidence only) · Evidence: boot `cargo build --release -p michi-server` binary on isolated temp data/config paths + scratch port (e.g. 18096) with throwaway admin; run contract `python3 tests/e2e/test_player_micro_contract_compatibility.py --url http://127.0.0.1:18096`; capture log; terminate server; confirm exit 0, `CONTRACT: OK`, port freed · Rollback: n/a · ~0 authored · Commit: included in 1.2–1.4 · Base: 01-audit-contract
- [x] **1.7** Regression guard — Deps: 1.6 · Files: none · Evidence: `cargo test --workspace` exit 0, 217/0/14 (no Rust change); `cargo fmt --check` + `cargo clippy --workspace --all-targets -- -D warnings` unchanged green · Rollback: n/a · ~0 authored · Commit: included in 1.2–1.5 · Base: 01-audit-contract

## Work Unit 2 — Slice 02: CI contract gate (PR 02, base: 01)

- [x] **2.0** WU1 native 4R review follow-ups on `tests/e2e/test_player_micro_contract_compatibility.py` — Deps: 1.4 · Files: `tests/e2e/test_player_micro_contract_compatibility.py` (same file) · Evidence: explicit negative auth guard (no-token + invalid-token to protected endpoint → 401 asserted; fail-open records FAIL and exits non-zero, never skips); HTTPError body decode guarded (non-JSON → raw-text fallback so error handling cannot crash); queue-transfer label aligned to empty-track_ids 400 semantics; docstring lists only exercised failure modes; `python3 -m py_compile` exit 0; real-server run → 33 passed/0 failed · Rollback: revert file · ~20 authored · Commit: `fix(contract): add negative auth guard and harden error-body decode` · Base: 02-ci-contract
- [x] **2.1** Create `scripts/ci_contract_gate.sh` — Deps: 1.4 · Files: `scripts/ci_contract_gate.sh` (new, ~55 lines) · Evidence: `set -euo pipefail`; requires server binary and python3 (missing → loud non-zero, never skip); builds binary; boots on isolated temp path + deterministic port with throwaway admin (`MICHI_AUTH_USERNAME/PASSWORD`, per-run password); polls public `/health/live` bounded 60s (never protected `/api/status`); runs contract test with 5s timeouts; writes failure log; trap/EXIT cleanup kills server + removes temp dir; non-zero propagation from boot/health/contract · Rollback: delete script · ~55 authored · Commit: `ci(contract): add contract gate script with health wait, cleanup, and failure propagation` · Base: 02-ci-contract
- [x] **2.2** Wire `ci-python-contract` job — Deps: 2.1 · Files: `.github/workflows/ci.yml` (add job `ci-python-contract`, `needs: ci-rust`) · Evidence: job checks out, runs `scripts/ci_contract_gate.sh`, uploads failure log artifact on failure; YAML parses; ci-rust/ci-docker untouched; no port collisions (deterministic per-run port + cleanup) · Rollback: remove job block · ~15 authored · Commit: `ci(contract): add ci-python-contract job gated on ci-rust` · Base: 02-ci-contract
- [x] **2.3** Local gate proof — Deps: 2.2 · Files: none (evidence) · Evidence: local equivalent of the job (build + boot + contract via script) exit 0 with log; force failure paths (dead server → health-wait error; contract FAIL → non-zero) each exit non-zero; no orphaned process or leaked port after trap · Rollback: n/a · ~0 authored · Commit: included in 2.1–2.2 · Base: 02-ci-contract

## Work Unit 3 — Slice 03: Docker/toolchain/ARM64 (PR 03, base: 02)

- [x] **3.0** WU2 native 4R review follow-ups on `scripts/ci_contract_gate.sh` — Deps: 2.1 · Files: `scripts/ci_contract_gate.sh` (same file) · Evidence: trap INT+TERM in addition to EXIT (signals tear down server + temp dir; signals ignored during cleanup so a second signal cannot abort teardown); health-poll curl `--connect-timeout 3 --max-time 5` per attempt; failure-log comment states default lives inside RUN_DIR unless `MICHI_FAILURE_LOG` overrides it; rebuild release binary when missing OR any workspace source/manifest newer than it (`find -newer`); `bash -n` exit 0; happy path exit 0 (33 passed/0 failed, stale-binary rebuild triggered); forced failure exit 1; TERM test exit 130 + server killed + temp dir removed · Rollback: revert file · ~24 authored · Commit: `ci(contract): harden gate signals, timeouts, and stale-binary rebuild` · Base: 03-build-release
- [x] **3.1** Re-derive Docker cache fix from HEAD — Deps: none hard · Files: `Dockerfile` (after `COPY apps ./apps; COPY crates ./crates`: refresh source/asset mtimes with `find apps crates -type f \( -name '*.rs' -o -name '*.html' -o -name '*.css' -o -name '*.js' -o -name '*.json' -o -name '*.svg' -o -name '*.png' -o -name '*.webp' \) -exec touch {} +` before final `cargo build --release --package michi-server`; COPY overwrites placeholders per design D6, so no rm — rm after COPY would delete the real sources) · Evidence: cached-deps rebuild ships real server (version 0.2.0), never no-op placeholder; re-authored from HEAD, not absorbed from working tree; placeholder pattern present in HEAD (verified) · Rollback: revert Dockerfile · ~9 authored · Commit: `build(docker): invalidate placeholder cache before final build` · Base: 03-build-release
- [x] **3.2** Pin toolchain 1.96.0 consistently — Deps: 3.1 · Files: `rust-toolchain.toml` (new, `channel = "1.96.0"`), `Dockerfile:1` `ARG RUST_VERSION=1.96.0` + `FROM rust:${RUST_VERSION}-bookworm` (bookworm variant keeps glibc matched to the bookworm-slim runtime), `.github/workflows/ci.yml` `dtolnay/rust-toolchain@stable` → pinned `toolchain: 1.96.0` input (both ci-rust and ci-python-contract) · Evidence: `rustc --version` = 1.96.0 locally; `cargo test --workspace` 217/0/14 green; builder `rust:1.96.0-bookworm` glibc 2.36 == runtime `debian:bookworm-slim` glibc 2.36; docs describe "reproducible channel", no MSRV claim · Rollback: delete toolchain file + revert pins · ~10 authored · Commit: `build(toolchain): pin Rust 1.96.0 across toolchain file, Docker, and CI` · Base: 03-build-release
- [x] **3.3** Multiarch release platforms + prerelease latest prohibition — Deps: 3.2 · Files: `.github/workflows/ci.yml` `release-ghcr` `platforms: linux/amd64` → `linux/amd64,linux/arm64`; keep meta `enable=${{ startsWith(github.ref, 'refs/tags/v') && !contains(github.ref, '-') }}` unchanged · Evidence: platforms line lists both; prerelease tag (contains `-`) never creates/updates `latest`; `contains(github.ref, '-')` guard retained (diff shows only the platforms line changed) · Rollback: revert platforms line · ~1 authored · Commit: `ci(release): publish amd64+arm64, keep latest non-prerelease-only` · Base: 03-build-release
- [x] **3.4** Docker smoke regression (local evidence) — Deps: 3.1–3.3 · Files: none (evidence) · Evidence: `docker build` OK; boot container; `/api/status` 200 `version 0.2.0` + `/health/live` 200 + root 200; boot log shows exactly 37 migrations (last: `migration 37: server config`); container reaches `healthy`; graceful stop (`docker stop -t 25`) → `received SIGTERM` → `WAL checkpoint complete` → `shutdown complete`, exit code 0; container + image removed · Rollback: n/a · ~0 authored · Commit: included in 3.1–3.3 · Base: 03-build-release
- [ ] **3.5** buildx arm64 + emulated boot local evidence — Deps: 3.4 · Files: none (evidence) · Evidence: NOT EXECUTED — ENVIRONMENT LIMITATION: host binfmt_misc has no QEMU arm64 handler; `docker run --rm --platform linux/arm64 alpine uname -m` and `docker buildx build --platform linux/arm64` both fail `exec /bin/sh: exec format error`. Never declare ARM64 qualified; wording recorded as "build + emulated boot only" is impossible in this environment · Rollback: n/a · ~0 authored · Commit: none · Base: 03-build-release
- [x] **3.6** Healthcheck probes public liveness endpoint — Deps: 3.4 · Files: `Dockerfile:91` (HEALTHCHECK CMD `/api/status` → `/health/live`; keep `wget -qO-`, `|| exit 1`, and all interval/timeout/start-period/retries values unchanged) · Evidence: P1 bug — `/api/status` sits in the protected auth router, so enabling `MICHI_AUTH_USERNAME`/`MICHI_AUTH_PASSWORD` made the built-in healthcheck 401 and the container permanently unhealthy. `docker build -t michi-v1s-smoke:wu3-hc .` OK; boot WITH auth enabled (throwaway admin) on isolated port 18097; `docker inspect … .State.Health.Status` = `healthy`; `/health/live` 200, `/api/status` 401 without token (auth active AND healthcheck public), root 200; graceful stop `docker stop -t 25` → SIGTERM → WAL checkpoint → shutdown complete, ExitCode 0; container + image removed, port freed. Persisted: `/tmp/opencode/michi-v1s/wu3-docker-hc-run.log` + `/tmp/opencode/michi-v1s/wu3-docker-hc-health.json` (password redacted) · Rollback: revert Dockerfile line · ~1 authored · Commit: `fix(docker): healthcheck probe public liveness endpoint` · Base: 03-build-release

## Work Unit 4 — Slice 04a: Receiver dedup, pure deletion exception (PR 04a, base: 03)

- [ ] **4.1** Delete dead duplicate `tests/receiver_simulator_integration.rs` — Deps: none · Files: delete `tests/receiver_simulator_integration.rs` (~305 lines) · Evidence: file gone; `git diff --stat` shows pure deletion; authoritative `crates/michi-receivers/tests/receiver_simulator_integration.rs` untouched · Rollback: restore file · ~−305 deletion · Commit: `test(receivers): remove dead duplicate receiver integration test` · Base: 04a-receiver-dedup
- [ ] **4.2** Delete dead duplicate `tests/e2e/test_receiver_simulator_integration.rs` — Deps: 4.1 · Files: delete `tests/e2e/test_receiver_simulator_integration.rs` (~305 lines) · Evidence: file gone; diff is deletion-only; crate copy untouched · Rollback: restore file · ~−305 deletion · Commit: included in 4.1 · Base: 04a-receiver-dedup
- [ ] **4.3** Regression: no behavior change — Deps: 4.2 · Files: none · Evidence: `cargo test --workspace` exit 0, still 217/0/14 (deleted files were never compiled); `git diff --stat` = 2 deletions only · Rollback: n/a · ~0 authored · Commit: included in 4.1 · Base: 04a-receiver-dedup

## Work Unit 5 — Slice 04b: Receiver runner repair (PR 04b, base: 04a)

- [ ] **5.1** Fix runner target + both sims + ignored — Deps: 4.3 · Files: `scripts/test_receiver_e2e.sh:43` — `cargo test --test receiver_simulator_integration -- --ignored` → `cargo test -p michi-receivers --test receiver_simulator_integration -- --ignored`, exporting both `MICHI_RECEIVER_SIM_URL` and `MICHI_RECEIVER_SIM_HIFI_URL` · Evidence: script runs from virtual-workspace root without error; both sim targets exercised with `-- --ignored`; run without sims → explicit non-zero "simulator not running" · Rollback: revert script · ~4 authored · Commit: `fix(receivers): target michi-receivers crate and both simulators in e2e runner` · Base: 04b-receiver-runner
- [ ] **5.2** Env-overridable sim paths — Deps: 5.1 · Files: `scripts/run_receiver_sim_standard.sh`, `scripts/run_receiver_sim_hifi.sh` (keep `MICHI_STREAM_SIM_PATH` override, machine default as fallback only; missing file → loud non-zero) · Evidence: with `MICHI_STREAM_SIM_PATH` set to a valid temp path the script uses it; missing → clear "simulator not found" error, exit non-zero · Rollback: revert scripts · ~4 authored · Commit: `fix(receivers): make simulator paths env-overridable with clear failure` · Base: 04b-receiver-runner
- [ ] **5.3** Loud unavailable-dependency failure proof — Deps: 5.2 · Files: none (evidence) · Evidence: with sims down, runner exits non-zero with explicit simulator-unavailable message; the 14 tests never reported as passing; no hang · Rollback: n/a · ~0 authored · Commit: included in 5.1 · Base: 04b-receiver-runner
- [ ] **5.4** Document simulator boundary truthfully — Deps: 5.3 · Files: `docs/STREAM_SIMULATOR_INTEGRATION.md` (external `pitydah/michi-music-stream` dependency, env vars, acquisition) · Evidence: docs state "runner repaired, CI enablement deferred"; no `ci-receivers` job added to `.github/workflows/ci.yml` (grep empty) — no receiver CI claim · Rollback: revert doc · ~25 authored · Commit: `docs(receivers): document simulator boundary and deferred CI enablement` · Base: 04b-receiver-runner

## Work Unit 6 — Slice 05: Metadata truthfulness (PR 05, base: 04b)

- [ ] **6.1** CasaOS version matches workspace — Deps: none · Files: `casaos/data.yml:2` `version: 0.1.0` → `0.2.0` · Evidence: `grep -n '^version:' casaos/data.yml` = 0.2.0; no other metadata (icon/screenshots/description) touched · Rollback: revert line · ~1 authored · Commit: `fix(metadata): align CasaOS version with workspace 0.2.0` · Base: 05-metadata
- [ ] **6.2** CHANGELOG migration count derived — Deps: none · Files: `CHANGELOG.md:37` "35 migraciones" → "37 migraciones" · Evidence: `grep -c 'fn migration_0' crates/michi-db/src/lib.rs` = 37 (derived, not invented); `grep -n migraciones CHANGELOG.md` = 37 · Rollback: revert line · ~1 authored · Commit: `fix(metadata): correct changelog migration count to 37` · Base: 05-metadata
- [ ] **6.3** No build/test impact — Deps: 6.1, 6.2 · Files: none · Evidence: `git diff --stat` shows only the two metadata files; `cargo check --workspace` unchanged green · Rollback: n/a · ~0 authored · Commit: included in 6.1–6.2 · Base: 05-metadata

---

## Chain and rollback notes

- Dependency diagram per child PR: mark current unit with 📍 and list parents; if previous slices appear in a child diff, retarget/rebase before review.
- Shared `ci.yml` edits (02 job, 03 platforms) must land in chain order 02 → 03; never both in one commit.
- Global rollback: revert slices in reverse order (05 → 04b → 04a → 03 → 02 → 01); no cross-slice schema/state coupling; no tag, no DB migration, no remote state mutated.
- Apply prerequisites: delivery strategy `force-chained` with `feature-branch-chain` already selected → no decision gate before apply; tracker worktree (0.1) and planning staging (0.2) are the first apply steps.
